name: "Set Fast Sleep"
# beacon_id: ""
# Beacon ID omitted - will prompt for interactive selection
actions:
  - name: set_fast_sleep
    type: set_sleep
    parameters:
      sleep: 1
      jitter: 0
